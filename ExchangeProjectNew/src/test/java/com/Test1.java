package com;

public class Test1 {
    public static void main(String[] args) {
//        Long totalTime = Long.parseLong(acceptByProjectNo.getPromiseDay().trim()) * Long.parseLong(handleDto.getDailyWorkingTime().trim());
        String str = "";
        String tt = str.trim();
        Long time = Long.parseLong(tt);
        System.out.println(time);
    }
}
